user  root;
worker_processes  1;

events {
    worker_connections  1024;
}

http {
    server {
        listen       80;
        server_name  localhost;

        location / {
            proxy_pass https://cn.bing.com/;
            # proxy_pass https://cn.bing.com;  # no '/' suffix
        }
    }
}
